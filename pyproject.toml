[project]
name = "code-guardian"
version = "0.0.0"
description = "Stop hook and review gates for Claude Code."
requires-python = ">=3.10"

[dependency-groups]
dev = ["pytest>=8", "pytest-xdist>=3"]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
