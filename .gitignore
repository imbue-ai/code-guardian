.claude/settings.local.json

# Reviewer state written when the guardian is run against this repo itself: the
# two runtime dirs every reviewed repo must ignore (see the README's setup
# notes), plus the local settings override the reviewer-* toggle skills write.
.reviewer/outputs/
.reviewer/logs/
.reviewer/settings.local.json

# Byte-compiled files, produced whenever anything imports the plugin scripts.
__pycache__/
*.pyc
